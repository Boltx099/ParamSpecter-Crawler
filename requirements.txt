# ParamSpecter v6.0 Requirements

# Core (required)
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0          # faster HTML parser

# DNS resolution -- used by SubdomainHunter for brute-force + record analysis
# Falls back to socket.gethostbyname() if missing, but performance is degraded
dnspython>=2.4.0

# Optional but recommended
tqdm>=4.66.0             # progress bars
colorama>=0.4.6          # Windows color support

# For JS rendering (optional, headless browser)
# playwright>=1.40.0     # pip install playwright && playwright install chromium
